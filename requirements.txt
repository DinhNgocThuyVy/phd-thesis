# Minimal Python dependencies for Quarto thesis rendering
# Only the packages actually used in the thesis

# Core data manipulation and analysis
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Data visualization 
matplotlib>=3.7.0,<4.0.0

# Jupyter ecosystem (required for Quarto)
jupyter>=1.0.0,<2.0.0
ipython>=8.12.0,<9.0.0
jupyter-cache>=0.6.0,<1.0.0  # Required for Quarto caching
